AdaGCN uses a shared encoder for source and target graphs, trains a discriminator with gradient penalty on the learned embeddings, and optimizes source classification plus domain alignment.

ADGFN now keeps that AdaGCN training structure intact and changes only two things:
1. After the shared AdaGCN encoder, it applies a separate learnable monomial graph filter to each domain.
2. It adds an adversarial probe-distribution game so the source and target filters are trained to align the pushforward of probe features, while the probe distributions try to make that alignment harder.

In short, AdaGCN learns domain-invariant embeddings with adversarial alignment. ADGFN keeps the same backbone, classifier, discriminator, and gradient-penalty workflow, but gives each domain its own learnable post-encoder filter and regularizes those filters through adversarial probe matching.


Let's create and run a new experiment. The goal is to see what actually affects the target performance for graph domain adaptation the most. The goal is to create a simGDA type of model and measure the target performance in different settings. So in this experiments we keep track of 3 domains. The source (whose labels we will directly use) the target (whose unlabeled data we use) and a reference (to track if the perforance improvement is due to generalization or a domain specific adaptation ~ so not used in training at all but only during evaluation). In the experiment we should use the train only the train mask whenever a training is envolved and val mask whenever we are evaluating performance. Case A) model is only trained with source (with CE loss) ~ mmd alignment weight = 0 Case B) model is trained with source label with CE loss and mmd alignment with target. Case C) Oracle model, trained with both source and traget labels (no MMD matching). For each case we kep track of the source, target and reference reference performances. Remember we only use the train mask for training and not the val mask (not the full data). We also want to compare the lipschitz ocnstant for each case by measuring the maximum gradient with respect to each data points at the end. So at the end we should track, for each model and for each source, target, reference pari in the airport dataset (which should have 6 permutations) we should train the three cases, then measure the source, target and reference performance, also the pairwise mmd distance for the features from their layers right before the classification layer. And to measure the lipschitz constant of each model, let's use the gradient of each model with respect to the data points. By measuring all these we should be able to reconstruct the domain adaptation bound saying Et <= Es + CgW1(Ps, Pt) + E_ideal from the spec reg paper. Look at my simgda implementation and other experiments. Create a whole directory for the related functions for this experiment and create a clean and reproducinble experiment that runs for multiple seeds and reports the average output, with multiple relevant plots all contained in a sinle director. And feel free to import functions from utils. 